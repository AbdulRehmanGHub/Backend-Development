<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EJS - Backend</title>
    <!-- <link rel="stylesheet" href="../stylesheets/style.css" /> -->
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
      <!-- <h2 class="text-2xl">Solved using EJS</h2>
      <div class="container flex flex-col items-center justify-center gap-3">
        
        <p class="text-md">hi 20 + 10 = <%= 20+10%></p>
        <p class="text-md">hi 100 - 10 = <%= 100-10%></p> -->

        <div class="bg-slate-900 text-white min-h-screen flex flex-col items-center justify-start py-10 gap-3">

          <h2 class="text-2xl font-semibold tracking-wider mb-5">-- File Creator --</h2>

        <form action="/create" method="post" class="flex flex-col items-center justify-center gap-2 max-w-[600px]">
          <input name="task_title" class="border-[1px] px-1 py-1 w-full outline-none" type="text" placeholder="Title here" required />
          <textarea name="task_details" class="border-[1px] px-1 py-1 w-full outline-none resize-none" placeholder="Details here" required></textarea>
          <input class="border-[1px] rounded-md w-[70%] outline-none bg-blue-700 hover:bg-blue-800 duration-300 mt-4 cursor-pointer" type="submit" value="Create File" required />
        </form>

        <hr class="h-1 text-slate-500 w-full">

        <span class="text-2xl">All Files</span>
        
        <div class="tasks flex flex-wrap items-center justify-center gap-6 gap-2 px-12 py-4">
        <% if(files.length > 0){ %>
        <% files.forEach((val)=>{ %>
              <div class="task min-w-40 shadow-md hover:shadow-xl shadow-zinc-900 px-2 py-1 bg-gray-700">
                <p class="text-lg font-semibold tracking-wide"><%= val %></p>
                <div class="flex items-center justify-between mt-3">
                  <a href="/file/<%= val %>" class="text-[.7rem] font-extralight">Read More...</a>
                  <a href="/edit/<%= val %>" class="bg-green-700 px-1 rounded-md hover:bg-green-800 duration-200 font-light">Update</a>
                </div>
              </div>
              <% }) %>
            </div>
           <% }else { %>
            <span class="text-slate-600">Currently there is no task available.</span>
           <% } %>
      </div>
    </div>
  </body>
</html>