<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BookStore</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-slate-800 text-slate-100">
    <div class="container px-10 min-h-screen">
      <h2 class="text-2xl font-semibold pt-8 pb-5">Book Details</h2>

      <a href="/books" class="inline-block my-6">
        <button
          class="border-[1px] border-black px-2 py-1 rounded-md bg-slate-950 hover:bg-slate-900 duration-300 cursor-pointer"
        >
          Back to Books
        </button>
      </a>

      <ul>
        <li><%= aBook.title %></li>
        <li><%= aBook.description %></li>
        <li><%= aBook.image %></li>
        <li><%= aBook.author %></li>
        <li><%= aBook.language %></li>
        <li>Rs. <%= aBook.price.toLocaleString() %></li>
      </ul>
      <div class="flex items-center gap-4">
        <a href="/book/<%=aBook._id%>/edit" class="inline-block my-6">
          <button
            class="border-[1px] border-black px-2 py-1 rounded-md bg-teal-800 hover:bg-teal-900 duration-300 cursor-pointer"
          >
            Edit Book
          </button>
        </a>
        <form method="post" action="/book/<%=aBook._id%>?_method=DELETE" class="inline-block my-6">
          <button
            class="border-[1px] border-black px-2 py-1 rounded-md bg-red-700 hover:bg-red-800 duration-300 cursor-pointer"
          >
            Delete Book
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
